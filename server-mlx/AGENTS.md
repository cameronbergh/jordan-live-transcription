# AGENTS.md

## Scope
This subdirectory is for the MLX backend track only.

## Rules
- Do not modify the NVIDIA backend path.
- Keep changes scoped to this MLX server track unless a shared doc update is truly necessary.
- Follow the shared API direction from `../BACKEND_API.md` as much as practical.
- If the best implementation differs from the NVIDIA path, document the difference clearly.

## Goal
Build a separate WebSocket-based transcription backend prototype for Apple Silicon / MLX-friendly deployment.
